# Root Environment Variables
# Copy to .env.local and fill in actual values

# Convex Configuration (shared by both storefront and admin)
# IMPORTANT: Must use VITE_ prefix for client-side code to access via import.meta.env
VITE_CONVEX_URL=https://your-convex-deployment.convex.cloud

# Also set CONVEX_URL for server-side code (used by convex backend)
CONVEX_URL=https://your-convex-deployment.convex.cloud

# API Keys (optional)
VITE_GEMINI_API_KEY=

# Feature Flags
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_COUPON_SYSTEM=true
VITE_ENABLE_AGE_VERIFICATION=true
