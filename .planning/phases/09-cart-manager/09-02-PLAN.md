---
phase: 09-cart-manager
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/storefront/index.tsx
  - apps/storefront/App.tsx
  - apps/storefront/components/CartModal.tsx
  - apps/storefront/components/ProductCard.tsx
  - apps/storefront/components/Layout.tsx
  - apps/storefront/components/header-components/HeaderActions.tsx
  - apps/storefront/components/header-components/MobileMenu.tsx
  - apps/storefront/components/checkout/OrderSummarySidebar.tsx
  - apps/storefront/components/checkout/ReviewStep.tsx
  - apps/storefront/pages/ProductPage.tsx
  - apps/storefront/pages/CheckoutPage.tsx
  - apps/storefront/pages/OrderConfirmationPage.tsx
  - apps/storefront/pages/DashboardPage.tsx
autonomous: true
requirements:
  - CART-05
  - CART-06
  - CART-07

must_haves:
  truths:
    - "All cart operations in all components use useCart() hook instead of Redux dispatch"
    - "Cart modal open/close uses useCartUI() context instead of Redux uiSlice"
    - "CartUIProvider wraps the app in the provider tree"
    - "Logout flow clears cart via useCart().clearCart() instead of Redux thunk"
    - "Coupon flow in CheckoutPage uses useCart() coupon methods instead of useCouponFlow()"
    - "Cart summary with rules in CartModal and CheckoutPage uses useCart() computed values instead of useCartSummaryWithRules()"
    - "No component imports from cartSlice.ts or convexCartBridge.ts"
  artifacts:
    - path: "apps/storefront/index.tsx"
      provides: "CartUIProvider added to provider tree"
      contains: "CartUIProvider"
    - path: "apps/storefront/App.tsx"
      provides: "Cleaned app with no cart sync effects"
      contains: "useCart"
    - path: "apps/storefront/components/CartModal.tsx"
      provides: "Cart modal using useCart() and useCartUI()"
      contains: "useCart"
    - path: "apps/storefront/pages/CheckoutPage.tsx"
      provides: "Checkout using useCart() for cart data and coupon flow"
      contains: "useCart"
  key_links:
    - from: "apps/storefront/components/CartModal.tsx"
      to: "apps/storefront/hooks/useCart.ts"
      via: "useCart() hook for items, removeItem, updateQuantity"
      pattern: "useCart\\(\\)"
    - from: "apps/storefront/components/CartModal.tsx"
      to: "apps/storefront/hooks/useCartUI.ts"
      via: "useCartUI() for closeCart modal"
      pattern: "useCartUI\\(\\)"
    - from: "apps/storefront/pages/CheckoutPage.tsx"
      to: "apps/storefront/hooks/useCart.ts"
      via: "useCart() for items, totals, coupon flow, clearCart"
      pattern: "useCart\\(\\)"
    - from: "apps/storefront/index.tsx"
      to: "apps/storefront/hooks/useCartUI.ts"
      via: "CartUIProvider wrapping app"
      pattern: "CartUIProvider"
---

<objective>
Migrate all 13 storefront components from Redux cart dispatch to the new `useCart()` and `useCartUI()` hooks. After this plan, NO component imports from `cartSlice.ts` or `convexCartBridge.ts`.

Purpose: This is the consumer-side of the cart manager migration. Plan 01 created the new hooks; this plan wires every component to use them. The Redux cart slice becomes dead code after this plan, ready for deletion in Plan 03.

Output: All storefront components using `useCart()`/`useCartUI()` instead of Redux dispatch. `CartUIProvider` in provider tree.
</objective>

<execution_context>
@/Users/danielshmuel.mirshukri/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/danielshmuel.mirshukri/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cart-manager/09-01-SUMMARY.md

Key files created by Plan 01 (must exist before this plan runs):
@apps/storefront/hooks/useCart.ts — Unified cart hook with addItem, removeItem, updateQuantity, clearCart, applyCoupon, removeCoupon, items, totals, etc.
@apps/storefront/hooks/useCartUI.ts — Cart modal context with isCartOpen, openCart, closeCart, toggleCart

Files being migrated (read these for current implementation):
@apps/storefront/App.tsx — Has two cart sync useEffects to remove
@apps/storefront/components/CartModal.tsx — Uses useCartSummaryWithRules, closeCartModal, removeFromCart, updateQuantity from Redux
@apps/storefront/components/ProductCard.tsx — Uses addToCart dispatch
@apps/storefront/components/Layout.tsx — Uses selectIsCartModalOpen from uiSlice
@apps/storefront/components/header-components/HeaderActions.tsx — Uses selectCartItemCount, toggleCartModal, handleCartLogout
@apps/storefront/components/header-components/MobileMenu.tsx — Uses handleCartLogout
@apps/storefront/pages/ProductPage.tsx — Uses addToCart dispatch, selectCartSubtotal
@apps/storefront/pages/CheckoutPage.tsx — Uses selectCartItems, clearCart, useCartSummaryWithRules, useCouponFlow
@apps/storefront/pages/OrderConfirmationPage.tsx — Uses clearCart dispatch
@apps/storefront/pages/DashboardPage.tsx — Uses handleCartLogout dispatch
@apps/storefront/index.tsx — Provider tree (add CartUIProvider here)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CartUIProvider and migrate core cart components</name>
  <files>apps/storefront/index.tsx, apps/storefront/App.tsx, apps/storefront/components/CartModal.tsx, apps/storefront/components/Layout.tsx, apps/storefront/components/ProductCard.tsx, apps/storefront/pages/ProductPage.tsx</files>
  <action>
**1. `apps/storefront/index.tsx` — Add CartUIProvider:**

Import `CartUIProvider` from `../hooks/useCartUI` (adjust relative path). Add it to the provider tree INSIDE `ConvexClientProvider` (needs Convex context) but can be inside or outside Redux `Provider`. Place it inside `Provider` for now (Redux still needed for uiSlice):

```
<ConvexClientProvider>
  <Provider store={store}>
    <CartUIProvider>
      <HelmetProvider>
        <App />
      </HelmetProvider>
    </CartUIProvider>
  </Provider>
</ConvexClientProvider>
```

**2. `apps/storefront/App.tsx` — Remove cart sync effects:**

Current state: Has two `useEffect` hooks — one that calls `dispatch(initializeCart(convex))` when auth state changes, and another that calls `syncCartToConvex(convex, cartItems, appliedCoupon)` when cart state changes. Both are now handled by the `useCart()` hook internally (it auto-merges on login, and Convex mutations are the source of truth for authenticated users).

Changes:
- Remove import of `useAppDispatch`, `useAppSelector` from `./store/hooks` (ONLY if no other usage remains in this file — check first)
- Remove import of `initializeCart`, `selectCartItems`, `selectAppliedCoupon` from `./store/slices/cartSlice`
- Remove import of `syncCartToConvex` from `./store/slices/convexCartBridge`
- Remove the two cart-related `useEffect` hooks
- Remove `const dispatch = useAppDispatch()`, `const cartItems = ...`, `const appliedCoupon = ...` (ONLY the cart-related ones)
- Keep everything else (routes, imports, etc.)
- If there's still a need to trigger the merge on login, the `useCart()` hook handles this internally via its auth transition `useEffect`. Just add `const {} = useCart();` at the top of App (or don't — the hook will be called by child components anyway). Actually, the merge effect in useCart needs to fire, so ensure at least ONE component mounts that calls `useCart()` — CartModal and Layout already do, so App.tsx doesn't need to call it explicitly.

**3. `apps/storefront/components/CartModal.tsx` — Full migration:**

Current imports to remove:
- `import { useAppDispatch } from '../store/hooks'`
- `import { removeFromCart, updateQuantity, useCartSummaryWithRules } from '../store/slices/cartSlice'`
- `import { closeCartModal } from '../store/slices/uiSlice'`

New imports:
- `import { useCart } from '../hooks/useCart'`
- `import { useCartUI } from '../hooks/useCartUI'`

Changes:
- Replace `const dispatch = useAppDispatch()` with `const cart = useCart()` and `const { closeCart } = useCartUI()`
- Replace `const cartSummary = useCartSummaryWithRules()` — use `cart.items`, `cart.subtotal`, `cart.shipping`, `cart.discount`, `cart.total`, `cart.itemCount`, `cart.appliedBenefits`, `cart.freeItems`, `cart.freeShippingThreshold`, `cart.amountUntilFreeShipping` directly from `cart`
- Replace `dispatch(removeFromCart(productId))` with `cart.removeItem(productId)`
- Replace `dispatch(updateQuantity({ productId, quantity }))` with `cart.updateQuantity(productId, quantity)`
- Replace `dispatch(closeCartModal())` with `closeCart()`
- Keep `useToast` if it exists (it stays on Redux/its own hook)

**4. `apps/storefront/components/Layout.tsx` — Cart modal state:**

Current:
- `import { useAppSelector } from '../store/hooks'`
- `import { selectIsCartModalOpen } from '../store/slices/uiSlice'`
- `const isCartOpen = useAppSelector(selectIsCartModalOpen)`

Replace with:
- `import { useCartUI } from '../hooks/useCartUI'`
- `const { isCartOpen } = useCartUI()`
- Remove `useAppSelector` import IF no other selectors used in this file

**5. `apps/storefront/components/ProductCard.tsx` — Add to cart:**

Current:
- `import { useAppDispatch } from '../store/hooks'`
- `import { addToCart } from '../store/slices/cartSlice'`
- `const dispatch = useAppDispatch()`
- `dispatch(addToCart({ productId, name, ... }))`

Replace with:
- `import { useCart } from '../hooks/useCart'`
- `const { addItem } = useCart()`
- `addItem(product, quantity)` — the useCart hook builds the CartItem from the product. The addItem signature takes a Product object (or the relevant fields). Check what Plan 01's useCart.addItem expects and match the call site. The current dispatch sends `{ productId, productNameHe, price, quantity, imageUrl }` — the new `addItem` should accept the full product object or the same fields. Adjust the call to match.

Keep `useToast` as-is.

**6. `apps/storefront/pages/ProductPage.tsx` — Add to cart + cart subtotal:**

Current:
- `import { useAppDispatch } from '../store/hooks'`
- `import { addToCart, selectCartSubtotal } from '../store/slices/cartSlice'`
- `import { useAppSelector } from '../store/hooks'`
- `const dispatch = useAppDispatch()`
- `const cartTotal = useAppSelector(selectCartSubtotal)`
- `dispatch(addToCart(...))`

Replace with:
- `import { useCart } from '../hooks/useCart'`
- `const cart = useCart()`
- `const cartTotal = cart.subtotal`
- `cart.addItem(product, quantity)`
- Remove all Redux imports

Keep `useToast` as-is.
  </action>
  <verify>
Run `grep -r "cartSlice\|convexCartBridge\|initializeCart\|syncCartToConvex" apps/storefront/index.tsx apps/storefront/App.tsx apps/storefront/components/CartModal.tsx apps/storefront/components/Layout.tsx apps/storefront/components/ProductCard.tsx apps/storefront/pages/ProductPage.tsx` — should return nothing. Run `pnpm --filter @apps/storefront type-check` — should pass.
  </verify>
  <done>
CartUIProvider wraps the app. App.tsx has no cart sync effects. CartModal uses useCart()/useCartUI(). Layout uses useCartUI(). ProductCard and ProductPage use useCart().addItem(). None of these files import from cartSlice or convexCartBridge.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate header, checkout, and remaining pages</name>
  <files>apps/storefront/components/header-components/HeaderActions.tsx, apps/storefront/components/header-components/MobileMenu.tsx, apps/storefront/pages/CheckoutPage.tsx, apps/storefront/pages/OrderConfirmationPage.tsx, apps/storefront/pages/DashboardPage.tsx, apps/storefront/components/checkout/ReviewStep.tsx</files>
  <action>
**1. `apps/storefront/components/header-components/HeaderActions.tsx`:**

Current Redux usage:
- `import { toggleCartModal, openSearchModal } from '../../store/slices/uiSlice'`
- `import { selectCartItemCount, handleLogout as handleCartLogout } from '../../store/slices/cartSlice'`
- `const dispatch = useAppDispatch()`
- `const cartCount = useAppSelector(selectCartItemCount)`
- `dispatch(handleCartLogout())`
- `dispatch(toggleCartModal())`
- `dispatch(openSearchModal())`

Changes:
- Import `useCart` from `../../hooks/useCart`
- Import `useCartUI` from `../../hooks/useCartUI`
- Replace `selectCartItemCount` with `const { itemCount, clearCart } = useCart()`
- Replace `dispatch(toggleCartModal())` with `toggleCart()` from `useCartUI()`
- Replace `dispatch(handleCartLogout())` with `clearCart()` — the handleLogout thunk just clears the cart. On logout, call `clearCart()` from useCart.
- **KEEP** `dispatch(openSearchModal())` — this stays on Redux uiSlice. Keep `useAppDispatch` import and the `dispatch` variable for this. Keep `import { openSearchModal } from '../../store/slices/uiSlice'`.
- Remove `import { selectCartItemCount, handleLogout as handleCartLogout } from '../../store/slices/cartSlice'`
- Remove `import { toggleCartModal } from '../../store/slices/uiSlice'` — but KEEP `openSearchModal` import from uiSlice.

**2. `apps/storefront/components/header-components/MobileMenu.tsx`:**

Current Redux usage:
- `import { useAppDispatch } from '../../store/hooks'`
- `import { handleLogout as handleCartLogout } from '../../store/slices/cartSlice'`
- `import { toggleMobileMenu, openSearchModal } from '../../store/slices/uiSlice'`
- `dispatch(handleCartLogout())`
- `dispatch(toggleMobileMenu())` and `dispatch(openSearchModal())`

Changes:
- Import `useCart` from `../../hooks/useCart`
- Replace `dispatch(handleCartLogout())` with `clearCart()` from useCart
- **KEEP** `dispatch(toggleMobileMenu())` and `dispatch(openSearchModal())` — these stay on Redux uiSlice.
- Remove `import { handleLogout as handleCartLogout } from '../../store/slices/cartSlice'`
- Keep `useAppDispatch`, `toggleMobileMenu`, `openSearchModal` imports (still needed for non-cart Redux actions).

**3. `apps/storefront/pages/CheckoutPage.tsx` — Full migration (most complex):**

Current Redux usage:
- `import { useAppSelector, useAppDispatch } from '../store/hooks'`
- `import { selectCartItems, clearCart, useCartSummaryWithRules, useCouponFlow } from '../store/slices/cartSlice'`
- `const dispatch = useAppDispatch()`
- `const cartSummary = useCartSummaryWithRules()` — provides items, subtotal, shipping, discount, total, etc.
- `const { validationState, appliedCoupon, handleValidateCoupon, handleApplyCoupon, handleRemoveCoupon } = useCouponFlow()`
- `dispatch(clearCart())`

Changes:
- Import `useCart` from `../hooks/useCart`
- Replace `const cartSummary = useCartSummaryWithRules()` with properties directly from `useCart()`:
  - `cart.items` instead of `cartSummary.items`
  - `cart.subtotal` instead of `cartSummary.subtotal`
  - `cart.shipping` instead of `cartSummary.shippingCost`
  - `cart.discount` instead of `cartSummary.discount`
  - `cart.total` instead of `cartSummary.total`
  - `cart.appliedBenefits` instead of `cartSummary.appliedBenefits`
  - `cart.freeItems` instead of `cartSummary.freeItems`
  - `cart.freeShippingThreshold` instead of `cartSummary.freeShippingThreshold`
  - `cart.amountUntilFreeShipping` instead of `cartSummary.amountUntilFreeShipping`
- Replace `useCouponFlow()` with coupon methods from `useCart()`:
  - `cart.applyCoupon` instead of `handleApplyCoupon`
  - `cart.removeCoupon` instead of `handleRemoveCoupon`
  - `cart.appliedCoupon` instead of `appliedCoupon`
  - For `handleValidateCoupon` — check if useCart exposes a validate function or if the validation is done within applyCoupon. Adjust call sites accordingly. The `OrderSummarySidebar` receives `onValidateCoupon`, `onApplyCoupon`, `onRemoveCoupon` as props — map these to cart methods.
- Replace `dispatch(clearCart())` with `cart.clearCart()`
- Remove all Redux imports from this file (useAppSelector, useAppDispatch, cartSlice imports)
- Keep `useToast` as-is

**4. `apps/storefront/pages/OrderConfirmationPage.tsx`:**

Current:
- `import { useAppDispatch } from '../store/hooks'`
- `import { clearCart } from '../store/slices/cartSlice'`
- `const dispatch = useAppDispatch()`
- `dispatch(clearCart())`

Replace with:
- `import { useCart } from '../hooks/useCart'`
- `const { clearCart } = useCart()`
- `clearCart()` directly
- Remove Redux imports

**5. `apps/storefront/pages/DashboardPage.tsx`:**

Current:
- `import { useAppDispatch } from '../store/hooks'`
- `import { handleLogout as handleCartLogout } from '../store/slices/cartSlice'`
- `const dispatch = useAppDispatch()`
- `dispatch(handleCartLogout())`

Replace with:
- `import { useCart } from '../hooks/useCart'`
- `const { clearCart } = useCart()`
- On logout: call `clearCart()` instead of `dispatch(handleCartLogout())`
- Remove Redux imports if no other Redux usage in this file

**6. `apps/storefront/components/checkout/ReviewStep.tsx`:**

Check this file's current cart imports. If it imports from cartSlice, migrate. If it only receives cart data as props from CheckoutPage, no changes needed. Based on the grep results, ReviewStep imports `CartItem` type from `@shared/types` (not from cartSlice), so it likely only needs props adjustments — verify and adjust if needed.

**Note on OrderSummarySidebar.tsx:** This component receives cart data and coupon handlers as PROPS (not from Redux directly). It doesn't import from cartSlice. The prop types should remain compatible since useCart() provides the same data shapes. No changes needed unless prop signatures don't match — verify during implementation.
  </action>
  <verify>
Run `grep -r "cartSlice\|convexCartBridge" --include="*.tsx" --include="*.ts" apps/storefront/components/ apps/storefront/pages/ apps/storefront/App.tsx apps/storefront/index.tsx` — should return ZERO results (only store/ files should still reference cartSlice). Run `pnpm --filter @apps/storefront type-check` — should pass.
  </verify>
  <done>
All 13 consumer files migrated. HeaderActions uses useCart() for count and cart toggle via useCartUI(), keeps Redux for search modal. MobileMenu uses useCart() for logout, keeps Redux for mobile menu toggle. CheckoutPage uses useCart() for all cart data, totals, and coupon flow. OrderConfirmationPage and DashboardPage use useCart().clearCart() for logout/confirmation. No component or page imports from cartSlice.ts or convexCartBridge.ts.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "from.*cartSlice\|from.*convexCartBridge" --include="*.tsx" --include="*.ts" apps/storefront/components/ apps/storefront/pages/ apps/storefront/App.tsx apps/storefront/index.tsx` — returns nothing
2. `grep -r "CartUIProvider" apps/storefront/index.tsx` — confirms provider is wired
3. `pnpm --filter @apps/storefront type-check` — no new errors
4. App.tsx has zero `initializeCart` or `syncCartToConvex` references
5. Every file that previously dispatched cart actions now calls useCart() methods
</verification>

<success_criteria>
- Zero imports from cartSlice in any component or page file
- Zero imports from convexCartBridge in any file outside store/
- CartUIProvider wraps the app in index.tsx
- App.tsx sync effects removed
- All cart operations go through useCart() hook
- All cart modal operations go through useCartUI() hook
- Type-checking passes
- Redux Provider still present (needed for uiSlice: search modal, mobile menu, toasts)
</success_criteria>

<output>
After completion, create `.planning/phases/09-cart-manager/09-02-SUMMARY.md`
</output>
