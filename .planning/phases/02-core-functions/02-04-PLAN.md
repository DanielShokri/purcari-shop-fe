---
phase: 02-core-functions
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified: [convex/coupons.ts]
autonomous: true
must_haves:
  truths:
    - "Coupons can be validated against current cart total and customer ID"
    - "Usage limits (global and per-user) are strictly enforced"
    - "Coupon status (active, expired, scheduled) is respected"
  artifacts:
    - path: "convex/coupons.ts"
      provides: "Coupon management and validation"
  key_links:
    - from: "convex/coupons.ts"
      to: "couponUsage table"
      via: "validation logic"
---

<objective>
Implement initial coupon management and validation logic. This ports the complex 12-step validation flow into a robust Convex action/mutation set.
</objective>

<execution_context>
@/Users/danielshmuel.mirshukri/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/danielshmuel.mirshukri/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@convex/schema.ts
@apps/storefront/services/api/couponsApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Coupon CRUD & Management</name>
  <files>convex/coupons.ts</files>
  <action>
    Create core management functions in `convex/coupons.ts`:
    - `list`: Admin-only list of coupons.
    - `get`: Fetch coupon by code.
    - `create/update/remove`: Admin-only management mutations.
    Ensure `usageCount` is initialized to 0 on create.
  </action>
  <verify>Create a coupon via CLI and verify its initial state in the dashboard.</verify>
  <done>Basic coupon data management is available.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Coupon Validation Logic</name>
  <files>convex/coupons.ts</files>
  <action>
    Implement the `validate` query in `convex/coupons.ts`:
    - Check if coupon exists and is `active`.
    - Verify `startDate` and `endDate`.
    - Check `minimumOrder` threshold.
    - Check `usageLimit` (global) and `usageLimitPerUser` (requires `userId`).
    - Return a structured result: `{ valid: boolean, discount?: number, error?: string }`.
    Port any remaining logic from the 12-step validation flow.
  </action>
  <verify>Test validation with: valid coupon, expired coupon, coupon below min order, and maxed-out usage.</verify>
  <done>Coupon validation logic is reliable and handles all schema-defined constraints.</done>
</task>

</tasks>

<verification>
Ensure coupons correctly apply discounts only when all criteria are met.
</verification>

<success_criteria>
Storefront can validate coupons. Admin can manage coupon lifecycle.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-functions/02-04-SUMMARY.md`
</output>
