---
phase: 08-system-announcements
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/admin/pages/SystemAnnouncements.tsx
  - apps/admin/components/systemAnnouncements/index.ts
  - apps/admin/components/systemAnnouncements/AnnouncementsTable.tsx
  - apps/admin/components/systemAnnouncements/AnnouncementEditor.tsx
  - apps/admin/components/layout-parts/routeConfig.ts
  - apps/admin/App.tsx
autonomous: true
requirements:
  - SYS-ANNOUNCE-03
  - SYS-ANNOUNCE-04
must_haves:
  truths:
    - "Admin can view all system announcements in a table"
    - "Admin can create new announcements with all fields"
    - "Admin can edit existing announcements"
    - "Admin can delete announcements"
    - "Admin can filter announcements by status"
    - "Admin sees a preview of how the banner will look"
  artifacts:
    - path: "apps/admin/pages/SystemAnnouncements.tsx"
      provides: "Main announcements management page"
      exports: ["default"]
    - path: "apps/admin/components/systemAnnouncements/AnnouncementsTable.tsx"
      provides: "Table component for listing announcements"
      exports: ["AnnouncementsTable"]
    - path: "apps/admin/components/systemAnnouncements/AnnouncementEditor.tsx"
      provides: "Dialog for creating/editing announcements"
      exports: ["AnnouncementEditor"]
    - path: "apps/admin/components/layout-parts/routeConfig.ts"
      provides: "Navigation route for announcements"
      contains: "הודעות מערכת"
    - path: "apps/admin/App.tsx"
      provides: "Route configuration for announcements page"
      contains: "/system-announcements"
  key_links:
    - from: "SystemAnnouncements.tsx"
      to: "api.systemAnnouncements"
      via: "useEntityList hook"
      pattern: "api\\.systemAnnouncements\\.(list|create|update|remove)"
    - from: "routeConfig.ts"
      to: "Sidebar.tsx"
      via: "navigationConfig"
      pattern: "system-announcements"
---

<objective>
Create admin panel page for managing system announcements with full CRUD functionality.

Purpose: Enable admins to create, edit, delete, and preview system announcements that appear as banners in the storefront.

Output:
- SystemAnnouncements.tsx main page
- AnnouncementsTable component
- AnnouncementEditor dialog component
- Navigation updates in routeConfig and App.tsx
</objective>

<execution_context>
@/Users/danielshmuel.mirshukri/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/danielshmuel.mirshukri/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference patterns from plan 08-01
@.planning/phases/08-system-announcements/08-01-SUMMARY.md

# Reference existing patterns
@apps/admin/pages/Coupons.tsx
@apps/admin/components/coupons/CouponsTable.tsx
@apps/admin/components/layout-parts/routeConfig.ts
@apps/admin/App.tsx
@apps/admin/hooks/useEntityList.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation route for System Announcements</name>
  <files>
    - apps/admin/components/layout-parts/routeConfig.ts
    - apps/admin/App.tsx
  </files>
  <action>
    Update navigation and routing:

    1. In `routeConfig.ts`:
       - Add route label: `'/system-announcements': 'הודעות מערכת'`
       - Add to navigationConfig under "ניהול" section:
         ```
         { to: '/system-announcements', icon: 'campaign', label: 'הודעות מערכת' }
         ```
       - Update getPageLabel to handle the new route

    2. In `App.tsx`:
       - Import SystemAnnouncements page
       - Add route:
         ```tsx
         <Route path="/system-announcements" element={
           <ProtectedRoute>
             <SystemAnnouncements />
           </ProtectedRoute>
         } />
         ```
       - Place after the notifications route
  </action>
  <verify>
    grep -q "system-announcements" apps/admin/components/layout-parts/routeConfig.ts &&
    grep -q "system-announcements" apps/admin/App.tsx
  </verify>
  <done>
    - Route label added for system announcements
    - Navigation item appears in sidebar under ניהול section
    - Route added to App.tsx with ProtectedRoute wrapper
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AnnouncementsTable component</name>
  <files>
    - apps/admin/components/systemAnnouncements/AnnouncementsTable.tsx
    - apps/admin/components/systemAnnouncements/index.ts
  </files>
  <action>
    Create the announcements table component following the pattern from CouponsTable.tsx:

    1. Create `apps/admin/components/systemAnnouncements/` directory

    2. Create `AnnouncementsTable.tsx` with:
       - Props interface matching CouponsTable pattern:
         - announcements: SystemAnnouncement[]
         - selectedAnnouncements: Set<string>
         - onSelectAll, onSelectAnnouncement callbacks
         - onEdit, onDelete callbacks
         - Pagination props (currentPage, totalPages, onPageChange, etc.)
       
       - Table columns (RTL):
         - Checkbox for selection
         - Status badge (פעיל/מתוזמן/פג תוקף/טיוטה with colors)
         - Title (כותרת) - bold text
         - Message preview (הודעה) - truncated text
         - Type badge (info/warning/success/error/maintenance)
         - Start date (תאריך התחלה)
         - End date (תאריך סיום) - optional
         - Actions menu (edit/delete)

       - Status badge colors:
         - active: emerald/green
         - scheduled: amber/yellow  
         - expired: slate/gray
         - draft: slate/gray

       - Type badge colors:
         - info: blue
         - warning: amber
         - success: green
         - error: red
         - maintenance: orange

    3. Create `index.ts` barrel export

    Use Chakra UI v3 components: Table, Badge, IconButton, Menu
  </action>
  <verify>
    ls apps/admin/components/systemAnnouncements/AnnouncementsTable.tsx &&
    grep -q "SystemAnnouncement" apps/admin/components/systemAnnouncements/AnnouncementsTable.tsx
  </verify>
  <done>
    - AnnouncementsTable component with all columns
    - Status and type badges with appropriate colors
    - Selection and action callbacks
    - Pagination support
    - Index barrel export
  </done>
</task>

<task type="auto">
  <name>Task 3: Create AnnouncementEditor dialog</name>
  <files>apps/admin/components/systemAnnouncements/AnnouncementEditor.tsx</files>
  <action>
    Create a dialog for creating/editing announcements following Chakra UI v3 Dialog pattern:

    1. Props interface:
       - isOpen: boolean
       - onClose: () => void
       - announcement?: SystemAnnouncement (for edit mode)
       - onSave: (data: CreateAnnouncementData) => void
       - isLoading?: boolean

    2. Form fields (use react-hook-form pattern from CouponEditor):
       - Title input (כותרת) - required, max 100 chars
       - Message textarea (הודעה) - required, max 500 chars
       - Type select: info/warning/success/error/maintenance
       - Status select: active/scheduled/expired/draft
       - Start date picker (תאריך התחלה) - date input
       - End date picker (תאריך סיום) - optional date input
       - Target audience select: all/customers/admins
       - Dismissible checkbox (ניתן לסגירה)

    3. Banner preview section:
       - Show live preview of how the banner will look
       - Match the styling from the user's mockup (amber warning style)
       - Update preview in real-time as form changes

    4. Dialog footer:
       - Cancel button
       - Save button (disabled when form invalid or loading)

    Use Chakra UI v3 components: Dialog, Field, Input, Textarea, Select, Checkbox
  </action>
  <verify>
    ls apps/admin/components/systemAnnouncements/AnnouncementEditor.tsx &&
    grep -q "AnnouncementEditor" apps/admin/components/systemAnnouncements/AnnouncementEditor.tsx
  </verify>
  <done>
    - Full CRUD dialog for announcements
    - Form validation with react-hook-form
    - Live banner preview
    - Hebrew labels for all fields
    - Cancel and Save buttons
  </done>
</task>

<task type="auto">
  <name>Task 4: Create SystemAnnouncements main page</name>
  <files>apps/admin/pages/SystemAnnouncements.tsx</files>
  <action>
    Create the main SystemAnnouncements page following the Coupons.tsx pattern:

    1. Use the useEntityList hook with:
       - query: api.systemAnnouncements.list
       - filters: search term, status filter
       - itemsPerPage: 10
       - enableSelection: true

    2. Page structure:
       - PageHeader with:
         - title: "הודעות מערכת"
         - subtitle: "ניהול והפצה של התראות והודעות לכלל משתמשי המערכת"
         - action button: "הודעה חדשה" with add icon
       
       - Filter toolbar:
         - Search input (חיפוש...)
         - Status filter dropdown (כל הסטטוסים/פעיל/מתוזמן/פג תוקף/טיוטה)
       
       - AnnouncementsTable component
       
       - AnnouncementEditor dialog (for create/edit)
       
       - DeleteConfirmationDialog

    3. State management:
       - Use useEntityList for list state
       - Use useState for editor dialog state (isOpen, editingAnnouncement)
       - Use useMutation for create/update/delete

    4. Handlers:
       - handleCreate: open editor in create mode
       - handleEdit: open editor with announcement data
       - handleDelete: open delete confirmation
       - handleSave: call create or update mutation based on mode

    Follow the exact pattern from Coupons.tsx for consistency.
  </action>
  <verify>
    ls apps/admin/pages/SystemAnnouncements.tsx &&
    grep -q "api.systemAnnouncements" apps/admin/pages/SystemAnnouncements.tsx
  </verify>
  <done>
    - Full CRUD page for system announcements
    - Search and status filtering
    - Table with pagination and selection
    - Create/edit dialog with live preview
    - Delete confirmation dialog
    - Cache-aware loading (hasEverLoaded pattern)
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm type-check` to verify no TypeScript errors
2. Navigate to /system-announcements in admin panel
3. Test create, edit, delete functionality
4. Verify banner preview updates in real-time
</verification>

<success_criteria>
- [ ] SystemAnnouncements page accessible at /system-announcements
- [ ] Navigation item appears in admin sidebar
- [ ] Table displays all announcements with status/type badges
- [ ] Create dialog works with all fields
- [ ] Edit dialog pre-populates existing data
- [ ] Delete confirmation works
- [ ] Search and status filtering functional
- [ ] Banner preview shows in editor dialog
- [ ] TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-system-announcements/08-02-SUMMARY.md`
</output>
