---
phase: 08-system-announcements
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - apps/storefront/components/SystemAnnouncementBanner.tsx
  - apps/storefront/components/Layout.tsx
autonomous: true
requirements:
  - SYS-ANNOUNCE-05
  - SYS-ANNOUNCE-06
must_haves:
  truths:
    - "User sees active announcement banner below header on storefront"
    - "Banner displays title and message in Hebrew"
    - "Dismissible banners can be closed by user"
    - "Closed banners stay dismissed (localStorage)"
    - "Banner styling matches announcement type (info/warning/etc)"
  artifacts:
    - path: "apps/storefront/components/SystemAnnouncementBanner.tsx"
      provides: "Banner component for displaying announcements"
      exports: ["SystemAnnouncementBanner"]
    - path: "apps/storefront/components/Layout.tsx"
      provides: "Integration point for banner in page layout"
      contains: "SystemAnnouncementBanner"
  key_links:
    - from: "SystemAnnouncementBanner.tsx"
      to: "api.systemAnnouncements.getActive"
      via: "useQuery hook"
      pattern: "api\\.systemAnnouncements\\.getActive"
    - from: "SystemAnnouncementBanner.tsx"
      to: "localStorage"
      via: "dismissedAnnouncements state"
      pattern: "localStorage|dismissed"
---

<objective>
Create and integrate the system announcement banner in the storefront.

Purpose: Display active system announcements as a dismissible banner below the header for all storefront visitors.

Output:
- SystemAnnouncementBanner component with type-based styling
- Layout.tsx integration to show banner on all pages
</objective>

<execution_context>
@/Users/danielshmuel.mirshukri/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/danielshmuel.mirshukri/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference patterns from plan 08-01 and 08-02
@.planning/phases/08-system-announcements/08-01-SUMMARY.md
@.planning/phases/08-system-announcements/08-02-SUMMARY.md

# Reference storefront patterns
@apps/storefront/components/Layout.tsx
@apps/storefront/components/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SystemAnnouncementBanner component</name>
  <files>apps/storefront/components/SystemAnnouncementBanner.tsx</files>
  <action>
    Create the banner component following the user's mockup design:

    1. Component structure:
       ```tsx
       import { useQuery } from 'convex/react';
       import { api } from '@convex/api';
       import { useState, useEffect } from 'react';
       import { SystemAnnouncement } from '@shared/types';
       ```

    2. Styling (match the user's amber warning mockup):
       - Container: `relative overflow-hidden rounded-lg border`
       - Type-specific colors:
         - info: `bg-blue-50 border-blue-200 dark:bg-blue-900/20 dark:border-blue-900/50`
         - warning: `bg-amber-50 border-amber-200 dark:bg-amber-900/20 dark:border-amber-900/50`
         - success: `bg-emerald-50 border-emerald-200 dark:bg-emerald-900/20 dark:border-emerald-900/50`
         - error: `bg-red-50 border-red-200 dark:bg-red-900/20 dark:border-red-900/50`
         - maintenance: `bg-orange-50 border-orange-200 dark:bg-orange-900/20 dark:border-orange-900/50`

    3. Layout (RTL):
       - Icon circle on right side
       - Title (bold) + Message text
       - Close button on left (only if isDismissible)
       - Colored stripe on right edge (4px width)

    4. Functionality:
       - Fetch active announcements via `api.systemAnnouncements.getActive`
       - Show only the first (most recent) active announcement
       - Track dismissed announcements in localStorage
       - Don't show if announcement ID is in dismissed list
       - On close: add announcement ID to localStorage and hide

    5. Icon mapping by type:
       - info: `info`
       - warning: `warning`
       - success: `check_circle`
       - error: `error`
       - maintenance: `build`

    Use Tailwind CSS with logical properties (start/end) for RTL support.
  </action>
  <verify>
    ls apps/storefront/components/SystemAnnouncementBanner.tsx &&
    grep -q "getActive" apps/storefront/components/SystemAnnouncementBanner.tsx &&
    grep -q "localStorage" apps/storefront/components/SystemAnnouncementBanner.tsx
  </verify>
  <done>
    - Banner component with type-based styling
    - Fetches active announcements from Convex
    - Dismissible state persisted to localStorage
    - RTL layout with Hebrew text
    - Material icons for announcement types
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate banner into storefront Layout</name>
  <files>apps/storefront/components/Layout.tsx</files>
  <action>
    Update the Layout component to include the SystemAnnouncementBanner:

    1. Import the component:
       ```tsx
       import SystemAnnouncementBanner from './SystemAnnouncementBanner';
       ```

    2. Place the banner BELOW the Header and ABOVE the main content:
       ```tsx
       <div className="flex flex-col min-h-screen font-sans">
         <Header />
         <SystemAnnouncementBanner />  {/* Add here */}
         <main className="flex-grow pt-20">
           {children}
         </main>
         <Footer />
         ...
       </div>
       ```

    3. The banner should be full-width but with container padding:
       - Wrap in a container div if needed for consistent spacing
       - Ensure it doesn't affect the pt-20 padding of main

    The banner will automatically show/hide based on active announcements and dismissal state.
  </action>
  <verify>
    grep -q "SystemAnnouncementBanner" apps/storefront/components/Layout.tsx
  </verify>
  <done>
    - Banner imported and integrated
    - Positioned below header, above main content
    - Full-width display with proper RTL support
    - No layout breakage on any page
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm type-check` to verify no TypeScript errors
2. Run storefront: `pnpm dev:storefront`
3. Create an active announcement in admin panel
4. Verify banner appears on storefront below header
5. Click dismiss button and verify banner disappears
6. Refresh page and verify banner stays dismissed
7. Test different announcement types for color changes
</verification>

<success_criteria>
- [ ] SystemAnnouncementBanner component created
- [ ] Type-based color styling matches design
- [ ] Banner fetches active announcements from Convex
- [ ] Dismissible functionality works with localStorage
- [ ] Banner integrated into Layout below header
- [ ] RTL layout works correctly
- [ ] TypeScript compilation passes
- [ ] No layout issues on any page
</success_criteria>

<output>
After completion, create `.planning/phases/08-system-announcements/08-03-SUMMARY.md`
</output>
